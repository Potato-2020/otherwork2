<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- rectangle表示为矩形 -->

    <!-- 填充的颜色 -->
    <gradient
        android:angle="0"
        android:endColor="@color/ps_color_transparent"
        android:startColor="@color/ps_color_transparent" />

    <!-- 边框的颜色和粗细 -->
    <stroke
        android:width="1dp"
        android:color="@color/white" />

    <!-- android:radius 圆角的半径 -->
    <corners
        android:radius="5dp" />

</shape>
